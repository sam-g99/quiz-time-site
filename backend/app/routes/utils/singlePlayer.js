const crypto = require('crypto');
const Quiz = require('../database/models/quizzes');
const Question = require('../database/models/questions');
const Option = require('../database/models/options');

module.exports.singlePlayer = (socket){
    // Setup a session  for single player in database

    // Start using socket to listen to events
    socket.on
    // End
}